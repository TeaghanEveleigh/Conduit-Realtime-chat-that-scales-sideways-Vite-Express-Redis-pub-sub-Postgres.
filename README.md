# Horizontally-Scaled-Realtime-Messaging-on-Vite-Express-Redis-Postgres
Realtime messaging app on Vite + Express + Redis + Postgres. Multi-instance backend with Redis pub/sub for cross-instance message routing, partitioned database, WebSocket presence and typing indicators, full E2E test coverage, deployed to Fly.io.
